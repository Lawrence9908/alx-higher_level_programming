-- Select all the record with score greater than or equal to 10.
-- The records will be ordered by score in descending order.
SELECT `score`, `name` FROM `second_table` WHERE score >= 10 ORDER BY score DESC;
